apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ include "argocd-mcp-server.fullname" . }}-priority
value: 100000
globalDefault: false
description: "High priority for MCP server pods"
